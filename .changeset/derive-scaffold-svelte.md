---
'create-tessera': patch
---

Scaffolded workspaces now derive their Svelte pin from the `tessera-learn` being installed, so the two can't drift when `tessera-learn`'s Svelte dependency is bumped.
