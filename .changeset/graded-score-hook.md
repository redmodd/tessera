---
'tessera-learn': minor
---

Expose the course-wide `gradedScore` and the effective `passingScore` on `useProgress()`, so a summary page can show the same score the LMS is sent. `passingScore` tracks an LMS masteryScore override, including in a custom layout.
