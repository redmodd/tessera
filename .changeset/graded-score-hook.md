---
'tessera-learn': minor
---

Expose the course-wide `gradedScore` and the effective `passingScore` on `useProgress()`, so a summary page can show the same score the LMS is sent. An LMS masteryScore override now reaches every reader of the pass threshold, including a custom layout.
