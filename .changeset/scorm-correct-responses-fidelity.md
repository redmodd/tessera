---
'tessera-learn': patch
---

Report fill-in correct responses faithfully: one pattern per acceptable answer in every standard, a `{case_matters=true}` prefix on each SCORM 2004 and xAPI pattern, a single pattern for `long-fill-in`, and a cap at the 10 patterns the RTE allows.
