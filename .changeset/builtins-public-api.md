---
'tessera-learn': minor
---

Built-in components use only the public API, which adds `useCourse()`, `resolveAsset`, `useQuiz().feedbackMode`/`maxAttempts`, `useNavigation().sections`/`prefetch`, and `useProgress().completedPages`.
