---
---

CI: add a `test:templates` end-to-end job that scaffolds each template, then
validates and builds it under every export standard (web / scorm12 / scorm2004 /
cmi5) against the locally-packed `tessera-learn`. Tooling only — no published
change.
