---
'tessera-learn': patch
---

Trim the quiz engine's internals: the index-keyed methods no longer appear in the shipped types, question handles come fully formed from the engine, and `useQuestion` warns when standalone-only options are set inside a quiz.
