---
'tessera-learn': minor
---

Read a page's own score with `useProgress().pageScore()`, including pages whose graded questions are standalone; an ungraded practice quiz no longer counts toward `gradedScore`.
