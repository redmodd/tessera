---
'tessera-learn': patch
---

`tessera a11y`/`tessera check` always rebuild before auditing (the `--build` flag is removed), and `tessera validate`'s accessibility tip now points at the working `pnpm a11y <course>` command.
