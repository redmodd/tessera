---
'tessera-learn': patch
---

Treat `xapi.endpoint: 'lms'` as an ignored entry with a build warning under scorm12/scorm2004/web, and leave the xAPI runtime out of those bundles, so one course config exports to every standard.
