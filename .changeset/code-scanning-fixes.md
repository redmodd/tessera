---
'tessera-learn': patch
'create-tessera': patch
---

fix: address CodeQL code-scanning alerts — arg-array `execFile` in test helpers, least-privilege CI workflow permissions, and a heading-order lint tidy-up. No runtime or API changes.
